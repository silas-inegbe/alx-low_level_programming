#include "main.h"
/**
 * _strncat - Concatenates two string using at most an inputed
 * @dest: destination
 * @src: source string
 * @n: integer
 * Return: A pointer to the resulting string
 */
char *_strncat(char *dest, char *src, int n)
{
	int = 0, dest_len = 0;

	while (dest[index++])
		det_len++;
	for (index = 0; src[index] && index < n; index++)
		dest[dest_len++] = src[index];
	return (dest);
}
